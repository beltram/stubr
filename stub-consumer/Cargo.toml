[package]
name = "stubr-consumer"
version = "0.0.1"
edition = "2021"
publish = false
build = "build.rs"

[build-dependencies]
stubr-build = { path = "../build", version = "0.4.14" }
stub-producer = { path = "../stub-producer" }
actix-producer = { path = "../actix-producer" }

[dev-dependencies]
stubr = { path = "../lib", version = "0.4.14", features = ["attributes"] }
asserhttp = { version = "0.4.0", features = ["isahc"] }
isahc = "1.7.0"
async-std = { version = "1.11.0", features = ["attributes"] }
