# see https://k3d.io/usage/configfile/#all-options-example
apiVersion: k3d.io/v1alpha2
kind: Simple
name: stubr
servers: 1
kubeAPI:
  host: "localhost"
  hostIP: "127.0.0.1"
  hostPort: "6445"
ports:
  - port: 8081:80
    nodeFilters:
      - loadbalancer
options:
  k3d:
    wait: true